version: 2
jobs:
  build:
    working_directory: ~/spotify-to-youtube
    docker:
      - image: circleci/node
    steps:
      - checkout
      - restore-cache:
        key: node_modules
      - run:
        name: test
        command: npm test
      - save-cache:
        key: node_modules
        paths:
          - node_modules